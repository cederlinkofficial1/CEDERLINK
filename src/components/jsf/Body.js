import React from "react";

const Body = () => {
    return(
        <div className="background-container">
            {/* Background image is applied via CSS */}
        </div>
    );
};

export default Body;